---
"@zemd/flickr-rest-api": major
---

Sync the `@zemd/http-client` dependency with its latest release. The updated client
changes the request building and response handling contracts, so this is a breaking
change for consumers of `@zemd/flickr-rest-api`.
